    'Do not use TODO test this code see if it work better
    'Private Sub SurroundingSub()
    '    Dim w As HttpWebRequest = CType(HttpWebRequest.Create("http://stackoverflow.com/favicon.ico"), HttpWebRequest)
    '    w.AllowAutoRedirect = True
    '    Dim r As HttpWebResponse = CType(w.GetResponse(), HttpWebResponse)
    '    Dim ico As System.Drawing.Image

    '    Using s As Stream = r.GetResponseStream()
    '        ico = System.Drawing.Image.FromStream(s)
    '    End Using

    '    ico.Save("favicon.ico")
    'End Sub
